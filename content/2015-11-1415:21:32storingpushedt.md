storing pushed timelines over the api using a scheduler instead of
==================================================================

Date: 2015-11-14 15:21:32

``` {style="white-space:pre-wrap;width:81ex"}
storing pushed timelines over the api using a scheduler instead of
storage during the livetime of the api request. That should speed up the
api access time.
```
