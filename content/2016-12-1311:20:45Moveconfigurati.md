Move configuration override to preload
======================================

Date: 2016-12-13 11:20:45

``` {style="white-space:pre-wrap;width:81ex"}
Move configuration override to preload

Both `start.sh` and `installation.sh` has the same procedure to
override configuration which overrides the script's variables into
what's defined on the configuration.
```
