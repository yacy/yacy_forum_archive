added CORS header to push servlet because loklak\_webclient is configured
=========================================================================

Date: 2015-10-15 11:43:09

``` {style="white-space:pre-wrap;width:81ex"}
added CORS header to push servlet because loklak_webclient is configured
to need that
```
