removed unused \'texts\' field (we create rich text tweets using the
====================================================================

Date: 2015-07-15 16:55:48

``` {style="white-space:pre-wrap;width:81ex"}
removed unused 'texts' field (we create rich text tweets using the
\n***\n separator) and added missing 'links' field which is already
written but not declared in the schema (declaring that will make the
function faster)
```
