enable fuzzy search, solr style (append a \~ to get a fuzzyness on the
======================================================================

Date: 2016-09-19 11:01:39

``` {style="white-space:pre-wrap;width:81ex"}
enable fuzzy search, solr style (append a ~ to get a fuzzyness on the
word)
```
