fix for behavior if constraint query if constraint is the only term in
======================================================================

Date: 2015-06-14 19:32:20

``` {style="white-space:pre-wrap;width:81ex"}
fix for behavior if constraint query if constraint is the only term in
the query
```
