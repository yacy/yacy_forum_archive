turned enum SourceType into a generic String (with some rules: must be
======================================================================

Date: 2016-06-06 12:20:35

``` {style="white-space:pre-wrap;width:81ex"}
turned enum SourceType into a generic String (with some rules: must be
uppercase, non-empty, longer than 3 chars, no space) to enable the easy
creation of more data sources; i.e. IoT device types.
```
