make RequestHeader login succeed (without throwing exception by default)
========================================================================

Date: 2016-11-25 01:09:42

``` {style="white-space:pre-wrap;width:81ex"}
make RequestHeader login succeed (without throwing exception by default)
correct getAuthType to return Auth Scheme only after authentication
```
