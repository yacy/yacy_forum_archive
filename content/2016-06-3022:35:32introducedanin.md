introduced an inference model, thoughts, and arguments as inference
===================================================================

Date: 2016-06-30 22:35:32

``` {style="white-space:pre-wrap;width:81ex"}
introduced an inference model, thoughts, and arguments as inference
proof model to susi in addition to the ability to apply patterns from
queries to the console. It is now possible to pass data entities to
console queries. An example is added to the cognition file.
Also, some javadoc was added to clean up the code.
```
