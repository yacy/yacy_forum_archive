added jackson json parser. This was not done because the
========================================================

Date: 2015-05-07 17:55:43

``` {style="white-space:pre-wrap;width:81ex"}
added jackson json parser. This was not done because the
elasticsearch-embedded json parser is so bad but because parts of the
loklak server shall become an independent library without elasticsearch
to be able to integrate it in other applications
```
