feeding of pre-defined queries into the automatic query harvester. To
=====================================================================

Date: 2015-11-04 21:39:23

``` {style="white-space:pre-wrap;width:81ex"}
feeding of pre-defined queries into the automatic query harvester. To
feed the harvester, files with queries in lines of a text file must be
placed into the data/queries folder. This will be read once upon startup
and imported into the local query database.
```
