moved the dump import process to it\'s own class. The dump import now
=====================================================================

Date: 2016-01-06 15:18:37

``` {style="white-space:pre-wrap;width:81ex"}
moved the dump import process to it's own class. The dump import now
also processes one file and then moves one file, not all as batch. The
separation of the dump import process from the caretaker makes room for
the other tasks that the caretaker does meanwhile.
```
