Add value \'unsupported\' as a response status for /api/validate.json
=====================================================================

Date: 2015-08-14 19:31:16

``` {style="white-space:pre-wrap;width:81ex"}
Add value 'unsupported' as a response status for /api/validate.json

There are some source types that exist but don't have json schema (yet). So validation can't be performed on these source types.
```
