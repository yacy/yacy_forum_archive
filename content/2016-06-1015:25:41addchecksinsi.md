add checks in signup servlet, make password configurable via config, let app get that parameters
================================================================================================

Date: 2016-06-10 15:25:41

``` {style="white-space:pre-wrap;width:81ex"}
add checks in signup servlet, make password configurable via config, let app get that parameters
```
