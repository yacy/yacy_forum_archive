Move process checking into preload script
=========================================

Date: 2016-12-13 11:19:30

``` {style="white-space:pre-wrap;width:81ex"}
Move process checking into preload script

Both `installation.sh` and `start.sh` has the same procedure to check
running processes of Loklak. It's a good idea to move them into the
new preload script.
```
