pushing a timeline can now contain a query string which updates the
===================================================================

Date: 2015-11-12 21:46:39

``` {style="white-space:pre-wrap;width:81ex"}
pushing a timeline can now contain a query string which updates the
query database with the latest retrieval statistics. This can be used
for remote retrieval agents to participate in harvesting
```
