Removes the surrounding DOM Wrapping when using characters rendering markdown
=============================================================================

Date: 2015-08-11 05:41:44

``` {style="white-space:pre-wrap;width:81ex"}
Removes the surrounding DOM Wrapping when using characters rendering markdown
```
